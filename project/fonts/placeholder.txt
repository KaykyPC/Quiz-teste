Esta pasta é para fontes personalizadas, se necessário.

Atualmente o projeto está usando Google Fonts (Inter) que é carregada via CDN no HTML.

Se você quiser usar fontes locais, adicione os arquivos .woff2, .woff, .ttf aqui e atualize o CSS.